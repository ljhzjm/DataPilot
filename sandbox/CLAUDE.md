# DataPilot Sandbox Rules

先阅读并遵守仓库根目录 `CLAUDE.md`。

- 本目录只能包含不可信代码执行所需的最小依赖和启动文件。
- 禁止加入数据库驱动、云 SDK、模型 SDK、凭据或网络客户端。
- 禁止使用 root 用户运行用户代码。
- 禁止把宿主机目录除任务级临时工作区外挂载进容器。
- 禁止以共享、长期存活的沙箱容器处理多个用户任务。
- 任何新增依赖都必须评估镜像体积和攻击面。

